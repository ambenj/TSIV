# Base outdirectory for blastp results
out_dir: /abs/path/

# Query protein fasta file
query_prot_fa: /abs/path/query_proteins.fa

# Directory with subject protein fasta files
subject_fa_dir: /abs/path/

# Prefixes for subject protein fasta files
subject_prot_fa_prefixes:
  - subject1
  - subject2
